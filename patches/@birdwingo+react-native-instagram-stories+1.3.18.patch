diff --git a/node_modules/@birdwingo/react-native-instagram-stories/src/components/Animation/Animation.styles.ts b/node_modules/@birdwingo/react-native-instagram-stories/src/components/Animation/Animation.styles.ts
index da69701..aabf23c 100644
--- a/node_modules/@birdwingo/react-native-instagram-stories/src/components/Animation/Animation.styles.ts
+++ b/node_modules/@birdwingo/react-native-instagram-stories/src/components/Animation/Animation.styles.ts
@@ -8,6 +8,6 @@ export default StyleSheet.create( {
     left: 0,
   },
   cube: {
-    justifyContent: 'center',
+    justifyContent: 'flex-start',
   },
 } );
diff --git a/node_modules/@birdwingo/react-native-instagram-stories/src/components/Header/Header.styles.ts b/node_modules/@birdwingo/react-native-instagram-stories/src/components/Header/Header.styles.ts
index 9419aa6..7098ccd 100644
--- a/node_modules/@birdwingo/react-native-instagram-stories/src/components/Header/Header.styles.ts
+++ b/node_modules/@birdwingo/react-native-instagram-stories/src/components/Header/Header.styles.ts
@@ -1,10 +1,10 @@
-import { StyleSheet } from 'react-native';
+import { StyleSheet, Platform } from 'react-native';
 
 export default StyleSheet.create( {
   container: {
     position: 'absolute',
     left: 16,
-    top: 32,
+    top: Platform.OS === 'ios' ? 60 : 32,
   },
   containerFlex: {
     flexDirection: 'row',
diff --git a/node_modules/@birdwingo/react-native-instagram-stories/src/components/Header/index.tsx b/node_modules/@birdwingo/react-native-instagram-stories/src/components/Header/index.tsx
index 32010ab..0ba2a35 100644
--- a/node_modules/@birdwingo/react-native-instagram-stories/src/components/Header/index.tsx
+++ b/node_modules/@birdwingo/react-native-instagram-stories/src/components/Header/index.tsx
@@ -1,57 +1,95 @@
-import React, { FC, memo } from 'react';
-import {
-  View, Text, Image, TouchableOpacity,
-  Pressable,
-} from 'react-native';
-import { WIDTH } from '../../core/constants';
-import HeaderStyles from './Header.styles';
-import { StoryHeaderProps } from '../../core/dto/componentsDTO';
-import Close from '../Icon/close';
+import React, { FC, memo } from "react";
+import { View, Text, Image, TouchableOpacity, Pressable } from "react-native";
+import { WIDTH } from "../../core/constants";
+import HeaderStyles from "./Header.styles";
+import { StoryHeaderProps } from "../../core/dto/componentsDTO";
+import Close from "../Icon/close";
+import { Ionicons } from "@expo/vector-icons";
 
-const StoryHeader: FC<StoryHeaderProps> = ( {
-  avatarSource, name, onClose, avatarSize, textStyle, closeColor, headerStyle,
-  headerContainerStyle, renderStoryHeader, onStoryHeaderPress,
-} ) => {
-
-  const styles = { width: avatarSize, height: avatarSize, borderRadius: avatarSize };
+const StoryHeader: FC<StoryHeaderProps> = ({
+  avatarSource,
+  name,
+  date,
+  onClose,
+  avatarSize,
+  textStyle,
+  closeColor,
+  headerStyle,
+  headerContainerStyle,
+  renderStoryHeader,
+  onStoryHeaderPress,
+  onMore,
+  userId,
+  ...rest
+}) => {
+  const styles = {
+    width: avatarSize,
+    height: avatarSize,
+    borderRadius: avatarSize,
+  };
   const width = WIDTH - HeaderStyles.container.left * 2;
 
-  if ( renderStoryHeader ) {
-
+  if (renderStoryHeader) {
     return (
-      <View
-        style={[ HeaderStyles.container, { width }, headerContainerStyle ]}
-      >
-        {renderStoryHeader()}
+      <View style={[HeaderStyles.container, { width }, headerContainerStyle]}>
+        {renderStoryHeader({ 
+          avatarSource, 
+          name, 
+          date, 
+          onClose, 
+          onMore,
+          userId,
+          ...rest 
+        })}
       </View>
     );
-
   }
 
   return (
-    <View style={[
-      HeaderStyles.container, HeaderStyles.containerFlex,
-      { width }, headerContainerStyle,
-    ]}
+    <View
+      style={[
+        HeaderStyles.container,
+        HeaderStyles.containerFlex,
+        { width },
+        headerContainerStyle,
+      ]}
     >
-      <Pressable style={[ HeaderStyles.left, headerStyle ]} onPress={() => onStoryHeaderPress?.()}>
-        {( Boolean( avatarSource ) ) && (
-          <View style={[ HeaderStyles.avatar, { borderRadius: styles.borderRadius } ]}>
+      <Pressable
+        style={[HeaderStyles.left, headerStyle]}
+        onPress={() => onStoryHeaderPress?.(userId)}
+      >
+        {Boolean(avatarSource) && (
+          <View
+            style={[HeaderStyles.avatar, { borderRadius: styles.borderRadius }]}
+          >
             <Image source={avatarSource!} style={styles} />
           </View>
         )}
-        {Boolean( name ) && <Text style={textStyle}>{name}</Text>}
+        <View style={{ flexDirection: "column", gap: 2 }}>
+          {Boolean(name) && <Text style={textStyle}>{name}</Text>}
+          {Boolean(date) && (
+            <Text style={[textStyle, { fontSize: 12, opacity: 0.8 }]}>
+              {date}
+            </Text>
+          )}
+        </View>
       </Pressable>
-      <TouchableOpacity
-        onPress={onClose}
-        hitSlop={16}
-        testID="storyCloseButton"
-      >
-        <Close color={closeColor} />
-      </TouchableOpacity>
+      <View style={{ flexDirection: "row", alignItems: "center", gap: 10 }}>
+        {onMore && (
+          <TouchableOpacity onPress={onMore}>
+            <Ionicons name="ellipsis-horizontal" size={24} color="#c4c4c4" />
+          </TouchableOpacity>
+        )}
+        <TouchableOpacity
+          onPress={onClose}
+          hitSlop={16}
+          testID="storyCloseButton"
+        >
+          <Close color={closeColor} />
+        </TouchableOpacity>
+      </View>
     </View>
   );
-
 };
 
-export default memo( StoryHeader );
+export default memo(StoryHeader);
diff --git a/node_modules/@birdwingo/react-native-instagram-stories/src/components/List/index.tsx b/node_modules/@birdwingo/react-native-instagram-stories/src/components/List/index.tsx
index 9fa47d8..1c62b34 100644
--- a/node_modules/@birdwingo/react-native-instagram-stories/src/components/List/index.tsx
+++ b/node_modules/@birdwingo/react-native-instagram-stories/src/components/List/index.tsx
@@ -1,6 +1,6 @@
-import React, { FC, memo } from 'react';
+import React, { FC, memo, useState, useEffect } from 'react';
 import Animated, {
-  useAnimatedStyle, useDerivedValue, useSharedValue, withTiming,
+  useAnimatedStyle, useDerivedValue, useSharedValue, withTiming, useAnimatedReaction, runOnJS,
 } from 'react-native-reanimated';
 import StoryAnimation from '../Animation';
 import ListStyles from './List.styles';
@@ -26,15 +26,32 @@ const StoryList: FC<StoryListProps> = ( {
     () => stories.findIndex( ( item ) => item.id === activeStory.value ),
   );
 
+  // Get current story date
+  const [currentStoryDate, setCurrentStoryDate] = useState<string | undefined>(undefined);
+
+  useAnimatedReaction(
+    () => activeStory.value,
+    (storyId) => {
+      if (storyId) {
+        const story = stories.find((item) => item.id === storyId);
+        if (story?.date) {
+          runOnJS(setCurrentStoryDate)(story.date);
+        } else {
+          runOnJS(setCurrentStoryDate)(undefined);
+        }
+      }
+    },
+    [stories]
+  );
+
   const animatedStyles = useAnimatedStyle( () => ( { height: imageHeight.value } ) );
   const contentStyles = useAnimatedStyle( () => ( {
     opacity: withTiming( hideElements.value ? 0 : 1 ),
   } ) );
 
   const onImageLayout = ( height: number ) => {
-
-    imageHeight.value = height;
-
+    // Keep it at full HEIGHT to prevent container resizing and shaking
+    imageHeight.value = HEIGHT;
   };
 
   const lastSeenIndex = stories.findIndex(
@@ -79,7 +96,7 @@ const StoryList: FC<StoryListProps> = ( {
               progressActiveColor={progressActiveColor}
               progressContainerStyle={progressContainerStyle}
             />
-            <StoryHeader {...props} />
+            <StoryHeader {...props} date={currentStoryDate} userId={id} />
             <StoryContent stories={stories} active={isActive} activeStory={activeStory} />
           </Animated.View>
         </Animated.View>
diff --git a/node_modules/@birdwingo/react-native-instagram-stories/src/components/Modal/gesture.tsx b/node_modules/@birdwingo/react-native-instagram-stories/src/components/Modal/gesture.tsx
index 640052c..bb73af4 100644
--- a/node_modules/@birdwingo/react-native-instagram-stories/src/components/Modal/gesture.tsx
+++ b/node_modules/@birdwingo/react-native-instagram-stories/src/components/Modal/gesture.tsx
@@ -1,10 +1,8 @@
 import React, { memo } from 'react';
-import { PanGestureHandler, PanGestureHandlerProps, gestureHandlerRootHOC } from 'react-native-gesture-handler';
+import { GestureDetector, GestureType } from 'react-native-gesture-handler';
 
-const GestureHandler = gestureHandlerRootHOC(
-  ( { children, onGestureEvent } : PanGestureHandlerProps ) => (
-    <PanGestureHandler onGestureEvent={onGestureEvent}>{children}</PanGestureHandler>
-  ),
+const GestureHandler = ( { children, gesture } : { children: React.ReactNode; gesture: GestureType } ) => (
+  <GestureDetector gesture={gesture}>{children}</GestureDetector>
 );
 
 export default memo( GestureHandler );
diff --git a/node_modules/@birdwingo/react-native-instagram-stories/src/components/Modal/index.tsx b/node_modules/@birdwingo/react-native-instagram-stories/src/components/Modal/index.tsx
index dd93855..2b73856 100644
--- a/node_modules/@birdwingo/react-native-instagram-stories/src/components/Modal/index.tsx
+++ b/node_modules/@birdwingo/react-native-instagram-stories/src/components/Modal/index.tsx
@@ -1,9 +1,9 @@
 import React, {
   forwardRef, memo, useEffect, useImperativeHandle, useState,
 } from 'react';
-import { GestureResponderEvent, Modal, Pressable } from 'react-native';
+import { GestureResponderEvent, Modal, Pressable, View } from 'react-native';
 import Animated, {
-  cancelAnimation, interpolate, runOnJS, useAnimatedGestureHandler, useAnimatedReaction,
+  cancelAnimation, interpolate, runOnJS, useAnimatedReaction,
   useAnimatedStyle,
   useDerivedValue, useSharedValue, withTiming,
 } from 'react-native-reanimated';
@@ -12,6 +12,7 @@ import {
 } from '../../core/constants';
 import { GestureContext, StoryModalProps, StoryModalPublicMethods } from '../../core/dto/componentsDTO';
 import GestureHandler from './gesture';
+import { Gesture } from 'react-native-gesture-handler';
 import StoryList from '../List';
 import ModalStyles from './Modal.styles';
 
@@ -20,7 +21,7 @@ const StoryModal = forwardRef<StoryModalPublicMethods, StoryModalProps>( ( {
   backgroundColor, videoProps, closeIconColor, modalAnimationDuration = STORY_ANIMATION_DURATION,
   storyAnimationDuration = STORY_ANIMATION_DURATION, hideElementsOnLongPress, loopingStories = 'none',
   statusBarTranslucent, loaderColor, loaderBackgroundColor, onLoad, onShow, onHide,
-  onSeenStoriesChange, onSwipeUp, onStoryStart, onStoryEnd, footerComponent, ...props
+  onSeenStoriesChange, onSwipeUp, onStoryStart, onStoryEnd, footerComponent, toast, ...props
 }, ref ) => {
 
   const [ visible, setVisible ] = useState( false );
@@ -261,25 +262,34 @@ const StoryModal = forwardRef<StoryModalPublicMethods, StoryModalProps>( ( {
 
   };
 
-  const onGestureEvent = useAnimatedGestureHandler( {
-    onStart: ( e, ctx: GestureContext ) => {
+  const gestureCtx = useSharedValue<GestureContext>( {
+    x: 0, pressedX: 0, pressedAt: 0, moving: false, vertical: false, userId: undefined,
+  } );
+
+  const panGesture = Gesture.Pan()
+    .onStart( ( e ) => {
 
-      ctx.x = x.value;
-      ctx.userId = userId.value;
+      gestureCtx.value = {
+        ...gestureCtx.value,
+        x: x.value,
+        userId: userId.value,
+      };
       paused.value = true;
 
-    },
-    onActive: ( e, ctx ) => {
+    } )
+    .onUpdate( ( e ) => {
+
+      const ctx = gestureCtx.value;
 
       if ( ctx.x === x.value
         && ( ctx.vertical || ( Math.abs( e.velocityX ) < Math.abs( e.velocityY ) ) ) ) {
 
-        ctx.vertical = true;
+        gestureCtx.value = { ...ctx, vertical: true };
         y.value = e.translationY / 2;
 
       } else {
 
-        ctx.moving = true;
+        gestureCtx.value = { ...ctx, moving: true };
         x.value = Math.max(
           0,
           Math.min( ctx.x + -e.translationX, WIDTH * ( stories.length - 1 ) ),
@@ -287,8 +297,10 @@ const StoryModal = forwardRef<StoryModalPublicMethods, StoryModalProps>( ( {
 
       }
 
-    },
-    onFinish: ( e, ctx ) => {
+    } )
+    .onEnd( ( e ) => {
+
+      const ctx = gestureCtx.value;
 
       if ( ctx.vertical ) {
 
@@ -338,14 +350,16 @@ const StoryModal = forwardRef<StoryModalPublicMethods, StoryModalProps>( ( {
 
       }
 
-      ctx.moving = false;
-      ctx.vertical = false;
-      ctx.userId = undefined;
+      gestureCtx.value = {
+        ...ctx,
+        moving: false,
+        vertical: false,
+        userId: undefined,
+      };
       hideElements.value = false;
       paused.value = false;
 
-    },
-  } );
+    } );
 
   const onPressIn = () => {
 
@@ -461,7 +475,7 @@ const StoryModal = forwardRef<StoryModalPublicMethods, StoryModalProps>( ( {
 
   return (
     <Modal statusBarTranslucent={statusBarTranslucent} visible={visible} transparent animationType="none" testID="storyRNModal" onRequestClose={onClose}>
-      <GestureHandler onGestureEvent={onGestureEvent}>
+      <GestureHandler gesture={panGesture}>
         <Animated.View style={ModalStyles.container} testID="storyModal">
           <Pressable
             onPressIn={onPressIn}
@@ -510,6 +524,11 @@ const StoryModal = forwardRef<StoryModalPublicMethods, StoryModalProps>( ( {
           {footerComponent && footerComponent}
         </Animated.View>
       </GestureHandler>
+      {toast && (
+        <View style={{ position: 'absolute', top: 0, left: 0, right: 0, bottom: 0, zIndex: 9999, pointerEvents: 'box-none' }}>
+          {toast}
+        </View>
+      )}
     </Modal>
   );
 
diff --git a/node_modules/@birdwingo/react-native-instagram-stories/src/components/Progress/Progress.styles.ts b/node_modules/@birdwingo/react-native-instagram-stories/src/components/Progress/Progress.styles.ts
index a265a10..37b7693 100644
--- a/node_modules/@birdwingo/react-native-instagram-stories/src/components/Progress/Progress.styles.ts
+++ b/node_modules/@birdwingo/react-native-instagram-stories/src/components/Progress/Progress.styles.ts
@@ -1,9 +1,9 @@
-import { StyleSheet } from 'react-native';
+import { StyleSheet, Platform } from 'react-native';
 
 export default StyleSheet.create( {
   container: {
     position: 'absolute',
-    top: 16,
+    top: Platform.OS === 'ios' ? 44 : 16,
     left: 16,
     height: 2,
     flexDirection: 'row',
diff --git a/node_modules/@birdwingo/react-native-instagram-stories/src/core/dto/componentsDTO.ts b/node_modules/@birdwingo/react-native-instagram-stories/src/core/dto/componentsDTO.ts
index c1a2ad0..491afd7 100644
--- a/node_modules/@birdwingo/react-native-instagram-stories/src/core/dto/componentsDTO.ts
+++ b/node_modules/@birdwingo/react-native-instagram-stories/src/core/dto/componentsDTO.ts
@@ -64,6 +64,7 @@ export interface StoryModalProps {
   hideOverlayViewOnLongPress?: boolean;
   loopingStories?: 'none' | 'all' | 'onlyLast';
   statusBarTranslucent?: boolean;
+  toast?: ReactNode;
   loaderColor?: string;
   loaderBackgroundColor?: string;
   onLoad: () => void;
@@ -138,11 +139,13 @@ export interface StoryProgressItemProps extends Omit<StoryProgressProps, 'length
 export interface StoryHeaderProps {
   avatarSource: ImageProps['source'];
   name?: string;
+  date?: string;
   avatarSize: number;
   textStyle?: TextStyle;
   closeColor: string;
   headerStyle?: ViewStyle;
   headerContainerStyle?: ViewStyle;
+  onMore?: () => void;
   onClose: () => void;
   renderStoryHeader?: () => ReactNode;
   onStoryHeaderPress?: () => void;
diff --git a/node_modules/@birdwingo/react-native-instagram-stories/src/core/dto/instagramStoriesDTO.ts b/node_modules/@birdwingo/react-native-instagram-stories/src/core/dto/instagramStoriesDTO.ts
index 263c8ea..966adef 100644
--- a/node_modules/@birdwingo/react-native-instagram-stories/src/core/dto/instagramStoriesDTO.ts
+++ b/node_modules/@birdwingo/react-native-instagram-stories/src/core/dto/instagramStoriesDTO.ts
@@ -70,6 +70,7 @@ export interface InstagramStoriesProps {
   onSwipeUp?: ( userId?: string, storyId?: string ) => void;
   onStoryStart?: ( userId?: string, storyId?: string ) => void;
   onStoryEnd?: ( userId?: string, storyId?: string ) => void;
+  onMore?: () => void;
 }
 
 export type InstagramStoriesPublicMethods = {
